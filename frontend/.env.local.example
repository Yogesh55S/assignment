# Public Client Environment Configuration Template
# Copy this file to .env.local in the client directory for local development.
# NEVER place backend secrets (JWT secrets, MongoDB URI, LLM keys) in client environment variables!

NEXT_PUBLIC_API_URL=http://localhost:5000
